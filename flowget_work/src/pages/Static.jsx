import React,{useEffect,useState} from 'react';import {getPage} from '../lib/store';
const titles={about:'About FlowGet',contact:'Contact Us',faq:'Frequently Asked Questions','privacy-policy':'Privacy Policy','terms-conditions':'Terms & Conditions','return-refund-policy':'Return & Refund Policy','delivery-information':'Delivery Information'};
export default function Static({slug}){const[p,setP]=useState(null),[loading,setLoading]=useState(true);useEffect(()=>{let alive=true;setLoading(true);getPage(slug).then(x=>{if(alive)setP(x)}).catch(()=>{}).finally(()=>alive&&setLoading(false));return()=>{alive=false}},[slug]);if(loading)return <div className="empty">Loading...</div>;return <div className="container"><div className="card"><h1 className="pageTitle">{p?.title||titles[slug]||'FlowGet'}</h1>{p?.content?<div className="pagecontent">{p.content}</div>:<p>Information for this page is being prepared. Please contact FlowGet for assistance.</p>}</div></div>}
