import React,{useEffect,useState} from 'react';import {supabase} from '../lib/supabase';import Header from './Header';import Footer from './Footer';
let navFn=(p)=>{if(!p)return;if(p.startsWith('http://')||p.startsWith('https://')){window.location.assign(p);return}window.history.pushState({},'',p);window.dispatchEvent(new PopStateEvent('popstate'));window.scrollTo({top:0,behavior:'instant'})};export const Link=({to,children,className,...props})=><a {...props} className={className} href={to} onClick={e=>{if(e.defaultPrevented||e.button!==0||e.metaKey||e.ctrlKey||e.shiftKey||e.altKey)return;e.preventDefault();navFn(to)}}>{children}</a>;export const useNavigate=()=>navFn;
export default function Layout({children}){const [settings,setSettings]=useState({store_name:'FlowGet',tagline:'Smart shopping for Bangladesh',primary:'#f97316',primary_hover:'#ea580c'});useEffect(()=>{import('../lib/store').then(m=>m.getSettings()).then(setSettings)},[]);useEffect(()=>{document.documentElement.style.setProperty('--primary',settings.primary||'#f97316');document.documentElement.style.setProperty('--primary-hover',settings.primary_hover||'#ea580c');document.title=settings.browser_title||settings.store_name||'FlowGet'},[settings]);return <><Header settings={settings}/><main>{children}</main><Footer settings={settings}/></>}
