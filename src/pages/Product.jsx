import React,{useEffect,useState}from'react';import{getProduct,getProducts}from'../lib/store';import{addCart}from'../lib/cart';import{ShoppingCart,Minus,Plus,Star}from'lucide-react';import{Link,useNavigate}from'../components/Layout';import ProductCard from'../components/ProductCard';
export default function Product({slug}){const navigate=useNavigate();const[p,setP]=useState(null),[qty,setQty]=useState(1),[related,setRelated]=useState([]),[activeImage,setActiveImage]=useState('');useEffect(()=>{let alive=true;getProduct(slug).then(async x=>{if(!alive)return;setP(x);setActiveImage(x?.main_image||'');if(x){document.title=`${x.name} | FlowGet`;let meta=document.querySelector('meta[name=description]');if(!meta){meta=document.createElement('meta');meta.name='description';document.head.appendChild(meta)}meta.content=(x.meta_description||x.description||`${x.name} — buy online from FlowGet.`).slice(0,155)}if(x?.category_id)setRelated((await getProducts({category:x.category_id})).filter(y=>y.id!==x.id).slice(0,5))}).catch(()=>setP(null));return()=>{alive=false}},[slug]);if(!p)return <div className="empty">Product not found.</div>;const price=p.discount_price||p.price;const gallery=[...(p.main_image?[{url:p.main_image,sort_order:-1}]:[]),...((p.product_images||[]).sort((a,b)=>Number(a.sort_order||0)-Number(b.sort_order||0)))].filter((x,i,a)=>x.url&&a.findIndex(y=>y.url===x.url)===i);const specObj=p.specifications&&typeof p.specifications==='object'?p.specifications:{};const specsText=typeof specObj.details==='string'?specObj.details:Object.entries(specObj).map(([k,v])=>`${k}: ${String(v??'')}`).join('\n');return <div className="container"><div className="detail"><div className="gallery">{activeImage?<img src={activeImage} alt={p.name}/>:<div className="placeholder">FLOWGET</div>}{gallery.length>1&&<div className="thumbs">{gallery.map((g,i)=><button type="button" className={activeImage===g.url?'active':''} key={g.url+i} onClick={()=>setActiveImage(g.url)}><img src={g.url} alt={`${p.name} ${i+1}`}/></button>)}</div>}{p.video_url&&<video className="productvideo" src={p.video_url} controls playsInline preload="metadata"/>}</div><div><div className="rating"><Star size={15} fill="currentColor"/> {p.rating||0} ({p.review_count||0})</div><h1>{p.name}</h1><p className="muted">SKU: {p.sku||'—'}{p.brand?' · '+p.brand:''}</p><div className="price">৳{price}<span className="old">{p.discount_price&&'৳'+p.price}</span></div>{p.short_description&&<p className="shortdescription">{p.short_description}</p>}<p><b>{p.stock>0?`In stock: ${p.stock}`:'Out of stock'}</b></p><div className="qty"><button onClick={()=>setQty(Math.max(1,qty-1))}><Minus size={16}/></button><span>{qty}</span><button onClick={()=>setQty(Math.min(p.stock||1,qty+1))}><Plus size={16}/></button></div><div style={{marginTop:15,display:'flex',gap:10,flexWrap:'wrap'}}><button className="btn" disabled={!p.stock} onClick={()=>{addCart(p,qty);alert('Added to cart')}}><ShoppingCart size={18}/> Add to Cart</button><button className="btn" disabled={!p.stock} onClick={()=>{addCart(p,qty);navigate('/checkout')}}>Buy Now</button><button type="button" className="btn secondarybtn" onClick={async()=>{try{await navigator.clipboard.writeText(location.href);alert('Product link copied')}catch{alert(location.href)}}}>Copy product link</button></div></div></div><section className="productinfo"><div className="productinfo-card" id="description"><h2>Product Description</h2><div className="productdescription">{p.description?<p>{p.description}</p>:<p>Quality product from FlowGet.</p>}</div></div><div className="productinfo-card" id="specifications"><h2>Specifications</h2>{specsText?<div className="productdescription">{specsText}</div>:<p className="muted">No specifications added for this product yet.</p>}</div></section>{related.length>0&&<section className="section"><h2>Related Products</h2><div className="grid">{related.map(x=><ProductCard p={x} key={x.id}/>)}</div></section>}</div>}
