import React,{useEffect,useState} from 'react';import {getPage} from '../lib/store';
const titles={about:'FlowGet সম্পর্কে',contact:'যোগাযোগ করুন',faq:'সাধারণ জিজ্ঞাসা','privacy-policy':'প্রাইভেসি পলিসি','terms-conditions':'শর্তাবলি','return-refund-policy':'রিটার্ন ও রিফান্ড পলিসি','delivery-information':'ডেলিভারি তথ্য'};
export default function Static({slug}){const[p,setP]=useState(null),[loading,setLoading]=useState(true);useEffect(()=>{let alive=true;setLoading(true);getPage(slug).then(x=>{if(alive)setP(x)}).catch(()=>{}).finally(()=>alive&&setLoading(false));return()=>{alive=false}},[slug]);if(loading)return <div className="empty">লোড হচ্ছে...</div>;return <div className="container"><div className="card"><h1 className="pageTitle">{p?.title||titles[slug]||'FlowGet'}</h1>{p?.content?<div className="pagecontent">{p.content}</div>:<p>এই পেজের তথ্য আপডেট করা হচ্ছে। প্রয়োজন হলে FlowGet-এর সাথে যোগাযোগ করুন।</p>}</div></div>}
