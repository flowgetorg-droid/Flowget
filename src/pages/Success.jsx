import React from'react';import{Check,Copy}from'lucide-react';import{Link}from'../components/Layout';export default function Success(){let x={};try{x=JSON.parse(sessionStorage.getItem('flowget_last_order')||'{}')}catch{};const id=x.order_id||x.id||'FG-XXXXXXXX';return <div className="container success"><div className="check"><Check/></div><h1>Order Confirmed</h1><p>Your order has been received successfully.</p><div className="card" style={{maxWidth:500,margin:'25px auto'}}><h3>Order ID</h3><h2>{id}</h2><button className="btn" onClick={()=>navigator.clipboard?.writeText(id)}><Copy size={16}/> Copy Order ID</button></div><Link className="btn" to="/track-order">Track Order</Link></div>}
